def shared_pods
  pod 'BigInt', '~> 2.1'
end

target 'TomatoCrypto-iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  platform :ios, '9.0'

  # Pods for TomatoCrypto-iOS
  shared_pods

  target 'TomatoCrypto-iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'TomatoCrypto-OSX' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  platform :osx, '10.10'

  # Pods for TomatoCrypto-OSX
  shared_pods

  target 'TomatoCrypto-OSXTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'TomatoCrypto-tvOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  platform :tvos, '10.10'

  # Pods for TomatoCrypto-tvOS
  shared_pods

  target 'TomatoCrypto-tvOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'TomatoCrypto-watchOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  platform :watchos, '10.10'

  # Pods for TomatoCrypto-watchOS
  shared_pods

end
